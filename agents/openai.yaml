interface:
  display_name: "Git Commit Guard"
  short_description: "Protect worktrees and write validated commits"
  default_prompt: "Use $git-commit-guard before editing this repository: inspect git status, protect existing work, validate changes, and create detailed Chinese commits when expected."
policy:
  allow_implicit_invocation: true
