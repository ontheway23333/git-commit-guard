interface:
  display_name: "Git Commit Guard"
  short_description: "Guard Git milestones and documentation lifecycle"
  default_prompt: "Use $git-commit-guard to protect the worktree, govern managed documentation transitions, validate each milestone, and create detailed Chinese commits."
policy:
  allow_implicit_invocation: true
